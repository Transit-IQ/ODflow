# Node / frontend
node_modules
dist
.env
.DS_Store

# Python / backend
venv/
__pycache__/
*.pyc
.pytest_cache/

# Raw GTFS + shapefiles — large local-only inputs, see data/README.md
# to populate this folder. Only that one README is tracked.
/data/*
!/data/README.md
